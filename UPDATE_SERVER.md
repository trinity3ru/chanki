# 🚀 Обновление сервера через GitHub

## 📦 Последнее обновление: Улучшение обработки ошибок

### Что нового:
- ✅ Увеличен таймаут HTTP-запросов с 10 до 15 секунд
- ✅ Добавлен механизм повторных попыток (retry): 3 попытки с интервалом 3 секунды
- ✅ Фильтрация ложных срабатываний: уведомления только после 2 последовательных ошибок
- ✅ Улучшена обработка временных ошибок (DNS, таймаут, сеть)
- ✅ Счетчик последовательных ошибок для каждого сайта

### Результат:
- 🔻 Значительно снижено количество ложных уведомлений
- ✅ Уведомления только при реальных проблемах с сайтами
- ✅ Устойчивость к временным сетевым сбоям

## 📋 Инструкции для обновления на сервере

### 1. Подключитесь к серверу
```bash
ssh root@your-server
cd /home/chanki
```

### 2. Обновите код из GitHub
```bash
git pull origin master
```

### 3. Перезапустите контейнер Docker
```bash
# Способ 1: Полный перезапуск (рекомендуется)
docker-compose down
docker-compose up -d --build

# Способ 2: Простой перезапуск (если build не нужен)
docker-compose restart
```

### 4. Проверьте логи
```bash
# Последние 20 строк логов
docker logs --tail 20 site-monitor-bot

# Логи в реальном времени
docker logs -f site-monitor-bot
```

### 5. Проверьте работу обновления
```bash
# Проверьте версию и статус контейнера
docker ps | grep site-monitor

# Проверьте что новые настройки применились
docker exec site-monitor-bot python -c "import config; print(f'Timeout: {config.REQUEST_TIMEOUT}s, Retries: {config.MAX_RETRIES}')"
```

### 6. Восстановите данные (если нужно)
Если файл `sites.json` все еще пустой:

```bash
# Скопируйте ваш файл sites.json в контейнер
docker cp sites.json site-monitor-bot:/app/host_data/sites.json

# Проверьте размер файла
docker exec site-monitor-bot ls -la /app/host_data/sites.json

# Перезапустите контейнер
docker-compose restart
```

## 🎯 Что исправлено в последних обновлениях

### В `site_monitor.py`:
- ✅ Механизм повторных попыток (retry) при ошибках подключения
- ✅ Фильтрация ложных срабатываний через счетчик последовательных ошибок
- ✅ Улучшена обработка таймаутов и временных сетевых ошибок
- ✅ Принятие HTTP-кодов 2xx и 3xx как успешных (редиректы)

### В `config.py`:
- ✅ Увеличен REQUEST_TIMEOUT с 10 до 15 секунд
- ✅ Добавлен RETRY_DELAY (3 секунды между попытками)
- ✅ Добавлен CONSECUTIVE_ERROR_THRESHOLD (2 ошибки перед уведомлением)

### В `database.py`:
- ✅ Добавлено поле `consecutive_errors` для отслеживания последовательных ошибок
- ✅ Инициализация счетчика при создании нового сайта

### В `telegram_bot.py` (предыдущие обновления):
- ✅ Команда `/remove` теперь поддерживает URL и имя сайта
- ✅ Можно удалять сайты по: ID, URL, домену, имени

## 🧪 Тестирование

После обновления протестируйте:

```bash
# Удаление по ID
/remove 8

# Удаление по домену
/remove promineral.ru

# Удаление по полному URL
/remove https://promineral.ru

# Удаление по имени
/remove promineral
```

## ⚠️ Важно

- Файл `sites.json` больше не будет перезаписываться при запуске
- При повреждении файла создается резервная копия
- Все изменения сохраняются между перезапусками
